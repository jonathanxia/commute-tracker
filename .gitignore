# JSON backups exported into the repo folder — real trip data, keep them local.
commute-backup-*.json

.DS_Store
.claude/settings.local.json

# Scratch coordination channel between concurrent agent sessions — never commit.
AGENTS.md
